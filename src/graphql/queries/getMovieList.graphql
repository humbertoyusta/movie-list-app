query getMovieList($id: Int!) {
    getMovieList(id: $id) {
        name
    }
    getMovieListItems(listId: $id) {
        id
        movie {
            imdbID
            Title
            Poster
            imdbRating
            Year
            Genre
        }
    }
}