import styled from "styled-components";

export const MovieResultsWrapper = styled.div`
    display: flex;
    flex-direction: row;
    position: relative;
    flex-wrap: wrap;
    width: 100%;
    padding: 2vw 3vw;
    gap: 3vw;
`;
